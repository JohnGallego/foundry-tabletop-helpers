{
    "name": "foundry-tabletop-helpers",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite build --watch",
        "build": "vite build",
        "zip": "npm run build && cd dist && zip -r ../foundry-tabletop-helpers.zip ."
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "vite": "^5.3.0"
    }
}